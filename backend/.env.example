PORT=5000

# Option 1: Cloud PostgreSQL Connection String (Recommended for Render, Neon, Supabase, Railway)
# DATABASE_URL=postgresql://user:password@ep-solar-db.region.aws.neon.tech/solar_admin_db?sslmode=require

# Option 2: Local or Self-Hosted PostgreSQL Parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=solar_admin_db
DB_USER=postgres
DB_PASSWORD=postgres
